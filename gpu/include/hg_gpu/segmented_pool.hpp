#pragma once

// Append-only segmented pool: atomic-bumped index allocator over a pre-sized
// device array. Mirrors the CPU SegmentedArray pattern in
// hypergraph/include/hypergraph/segmented_array.hpp.
// Implementation in M2.6.
