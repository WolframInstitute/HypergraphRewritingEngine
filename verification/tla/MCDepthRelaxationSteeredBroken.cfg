\* Claims compare against the run's budget while a steered pass holds the gate lower. Expected: VIOLATION.
\* A node claimed at a depth between the two is deferred at the gate; the resume after the gate
\* rises takes the claim first, finds it taken, and the node is never expanded.
CONSTANT LiveMinimum = TRUE
CONSTANT OneBound = FALSE
CONSTANT Budget = 3
CONSTANT PassBudget = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT DepthNeverBelowShortestPath
INVARIANT ExpandedIsShortestPathBounded
